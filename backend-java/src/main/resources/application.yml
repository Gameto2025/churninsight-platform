server:
  port: 8080

spring:
  application:
    name: churninsight-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
